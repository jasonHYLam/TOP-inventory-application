extends layout 

block content 
    h1=title

    p Artists added: #{artist_list.length}

    if artist_list.length > 0
        ul 
            each artist in artist_list
                li 
                    a(href=artist.url) #{artist.name}
    else 
        p No artists.
    
    hr